$pairs = ("one", "1"), ("two", "2"), ("three", "3"), ("four", "4"), ("five", "5"), ("six", "6"), ("seven", "7"), ("eight", "8"), ("nine", "9"), ("[^\d]", ""); (Get-Clipboard).Split('\n') | % { $line = $_; ($pairs | % { $line = $line -replace $_[0], $_[1] }); $line } | % { $_[0] + $_[$_.length - 1] } | % { [System.Convert]::ToInt32($_) }
